<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #212121;
        margin: 0;
        padding: 0;
        display: flex;
        /* background-image: url("https://source.unsplash.com/featured/?coding"); Adjusted URL with additional coding-related keywords */
        background-size: cover; /* Cover the entire background */
        background-position: center; /* Center the background image */
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      form {
        background-color: transparent; /* Semi-transparent white background */
        border-radius: 10px;
        box-shadow: 0 0 50px rgba(151, 147, 147, 0.5); /* Improved box shadow */
        padding: 40px;
        width: 320px;
      }

      .form-title {
        text-align: center;
        margin-bottom: 20px;
        font-size: 36px;
        color: #e8e8e8; /* Blue color for the title */
        /* background-color: #a9bdd3; White background */
        padding: 10px 0;
        border-radius: 5px;
        /* box-shadow: 0 0 10px rgba(0, 123, 255, 0.5); Shadow effect */
        transition: text-decoration 0.3s, color 0.3s; /* Transition for text decoration and color */
        text-decoration: underline;
        font-weight: bold;
      }
      input[type="text"],
      input[type="password"] {
        width: calc(100% - 24px);
        padding: 12px;
        margin: 8px 0;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
        font-size: 16px;
      }

      input[type="text"]:focus,
      input[type="password"]:focus {
        border-color: #007bff; /* Change border color on focus */
      }

      button {
        background-color: #007bff;
        color: #fff;
        border: none;
        padding: 14px 0;
        border-radius: 5px;
        cursor: pointer;
        font-size: 18px;
        width: 100%;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #0056b3;
      }
      .usernamelabel {
        display: block;
        color: white;
        font-size: 14px;
        padding: 5px 5px;
      }

      #usernameField {
        display: block;
        width: 200px;
        height: 40px;
        background-color: #292929;
        border-radius: 30px;
        border: 2px solid #292929;
        padding: 0px 12px;
        outline: none;
        caret-color: rgb(152, 88, 255);
        color: rgb(212, 212, 212);
        font-size: 12px;
        transition-duration: 0.2s;
      }

      #usernameField:focus,
      #usernameField:valid {
        border: 2px solid rgb(152, 88, 255);
        transition-duration: 0.2s;
      }
    </style>
  </head>
  <body>
    <form method="POST" action="/home">
      <h2 class="form-title" id="loginTitle">Welcome!!</h2>
      <label for="usernameField" class="usernamelabel label">Username</label>
      <input
        autocomplete="off"
        type="text"
        placeholder="@"
        id="usernameField"
        name="username"
        required="required"
      />
      <label for="usernameField" class="usernamelabel label">password</label>
      <input
        autocomplete="off"
        type="password"
        placeholder="password"
        id="usernameField"
        name="password"
        required="required"
      />
      <button type="submit">Log In</button>
    </form>
  </body>
</html>
